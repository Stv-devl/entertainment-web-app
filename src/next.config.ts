/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;

//add .env
/*
module.exports = {
  env: {
    customKey: 'my-value',
  },
};*/
